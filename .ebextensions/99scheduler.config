container_commands:
  runLeaderSelectionCRON:
    command: "php artisan system:start:leaderselection" # Adds a cron entry that checks for leader selection every 5 minutes
    test: "1=2"
  runLeaderSelection:
    command: "php artisan aws:configure:leader" # Does an initial leader selection check
    test: "1=2"
